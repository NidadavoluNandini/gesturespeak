flask
flask-cors
flask-socketio
eventlet
gunicorn
numpy
opencv-python-headless
mediapipe
tflite-runtime
# If tflite-runtime won't install on the build image, replace the last line with:
# tensorflow-cpu==2.11.0
