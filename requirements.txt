# Web server
flask
flask-cors
flask-socketio
eventlet
gunicorn

# ML + CV dependencies
numpy==1.24.3
opencv-python-headless==4.8.1.78   # ✅ Headless build (no GUI, no libGL.so.1 issue)
mediapipe==0.10.14
tensorflow==2.14.0
scikit-learn==1.3.0
pickle-mixin

# Prevent pip from pulling GUI opencv
opencv-python==0.0.0
